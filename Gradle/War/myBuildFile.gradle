//gradle -q -b Gradle/War/myBuildFile.gradle hello 的输出
//使用 -b 参数选择其他目录的构建文件
task hello << {
    println "using build file '$buildFile.name' in '$buildFile.parentFile.name'."
    //输出 using build file 'myBuildFile.gradle' in 'War'.

}

//gradle -q -p Gradle/War hello 命令的输出
//using build file 'myBuildFile.gradle' in 'War'.
//hello war!
//-b 参数用以指定脚本具体所在位置, 格式为 dirpwd/build.gradle.
//-p 参数用以指定脚本目录即可.
