<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.adam.sys.userfunc.dao.UserDao" >
  <resultMap id="BaseResultMap" type="com.adam.sys.userfunc.entity.User" >
    <id column="id" property="id" jdbcType="INTEGER" />
    <result column="username" property="username" jdbcType="INTEGER" />
    <result column="pwd" property="pwd" jdbcType="VARCHAR" />
    <result column="status" property="status" jdbcType="INTEGER" />
    <result column="fullname" property="fullname" jdbcType="VARCHAR" />
    <result column="creator" property="creator" jdbcType="VARCHAR" />
    <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
    <result column="updator" property="updator" jdbcType="VARCHAR" />
    <result column="update_time" property="updateTime" jdbcType="TIMESTAMP" />
    <result column="is_deleted" property="isDeleted" jdbcType="INTEGER" />
  </resultMap>
  <sql id="Base_Column_List" >
    id, username, pwd, `status`, fullname, creator, create_time, updator, update_time,  is_deleted
  </sql>
  
  <select id="queryCheckUserName" parameterType="string" resultType="int">
    select count(1) from t_sys_user where username=#{username}
  </select>
  
  <select id="queryLogin" resultMap="BaseResultMap" parameterType="map" >
    select <include refid="Base_Column_List" />from t_sys_user
    where is_deleted=0 and status=1 and username=#{username} and pwd=#{pwd}
  </select>
  
  <select id="queryAll" resultMap="BaseResultMap" parameterType="map">
    select  
    <include refid="Base_Column_List" /> 
    from t_sys_user
    where is_deleted=0
    limit #{start},#{rows}
  </select>
  
  <select id="queryAllCount" resultType="int">
     select count(1) from t_sys_user where is_deleted=0
  </select>
  
  <update id="delete" parameterType="com.adam.sys.userfunc.entity.User" >
    update t_sys_user set is_deleted=1,updator=#{updator},update_time=NOW() where id = #{id,jdbcType=INTEGER}
  </update>
  <insert id="insert" parameterType="com.adam.sys.userfunc.entity.User" useGeneratedKeys="true" keyProperty="id">
    insert into t_sys_user (username, pwd, status, fullname, creator, create_time, updator, update_time, is_deleted)
    values (#{username,jdbcType=VARCHAR}, #{pwd,jdbcType=VARCHAR}, 
      #{status,jdbcType=INTEGER}, #{fullname,jdbcType=VARCHAR}, #{creator,jdbcType=VARCHAR}, 
      NOW(), #{updator,jdbcType=VARCHAR},NOW(), 0)
  </insert>
  
  <update id="update" parameterType="com.adam.sys.userfunc.entity.User" >
    update t_sys_user
    set username = #{username,jdbcType=VARCHAR},
      status = #{status,jdbcType=INTEGER},
      fullname = #{fullname,jdbcType=VARCHAR},
      updator = #{updator,jdbcType=VARCHAR},
      update_time = NOW()
    where id = #{id,jdbcType=INTEGER}
  </update>
  
  <update id="updatepwd" parameterType="com.adam.sys.userfunc.entity.User">
     update t_sys_user set pwd=#{pwd} 
     <where>
     	<if test="id != null and id != ''">
     		id=#{id}
     	</if>
     	<if test="username != null and username != ''">
     		and username = #{username}
     	</if>
     </where> 
     
  </update>
</mapper>